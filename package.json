{
  "name": "health-agg",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "vitest run",
    "gen:types": "supabase gen types typescript --local --schema public > packages/types/database.types.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "typescript": "^5",
    "vitest": "^3.1.0"
  }
}
